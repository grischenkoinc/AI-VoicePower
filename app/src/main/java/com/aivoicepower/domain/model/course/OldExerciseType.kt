package com.aivoicepower.domain.model.course

// Temporary compatibility enum - will be removed in future phases
enum class ExerciseType {
    PRONUNCIATION,
    READING,
    BREATHING,
    ARTICULATION,
    PRESENTATION
}
